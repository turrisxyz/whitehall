<% page_title "New person" %>

<% content_for :title, "New person" %>
<% content_for :back_link, admin_people_path %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render "govuk_publishing_components/components/inset_text", {
      text: "Do not create ministerial people without consulting GDS."
    } %>
    <%= render partial: "form", locals: {person: @person, show_consult_gds_warning: true} %>
  </div>
</div>
